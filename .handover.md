---
context_pct: 0
timestamp: "2026-04-21T16:10:00+0800"
task_id: "coding-supervisor-v1"
pending: false
---

## Current Status
coding-supervisor skill v1 设计 + CLI 迁移已完成并通过冒烟测试。PROJECT.md 已更新。未进入真实 story 试跑阶段；未做 git 提交。

## Completed Work
- `skills/coding-supervisor/SKILL.md` — 三阶段主流程（Phase 1 Init / Phase 2 Wave Loop / Phase 3 E2E+Self-Eval），Mode 表，Agents 复用 orchestrator reviewer
- `skills/coding-supervisor/templates/task.md` — 三段模板 Objective / Protocol / Acceptance Checklist
- `skills/coding-supervisor/templates/tasks.yaml` — status 枚举简化为 `pending|executing|completed|blocked`，无 usage 块
- `skills/coding-supervisor/templates/story.md` — 加 Mode 字段 + Explore Result 表
- `skills/coding-supervisor/references/task-decomposition.md` — 新增，保留 Rules 1/2/3（Test Layer / No Orphans / Vertical Slice）
- `skills/coding-supervisor/references/self-evaluation.md` — 新增，story-summary.md 5 节模板
- `skills/coding-supervisor/references/story-memory-guideline.md` — 改：去 handoff 引用，区分 inline vs multi_wave 写入权限
- `skills/coding-supervisor/references/commands.md` — 保留原样（multi_wave 用）
- 删除 `skills/coding-supervisor/references/handoff-guideline.md` 和 `templates/handoff-context.yaml`
- `skills/coding-supervisor/scripts/` — common.py / archive.py / story.py(仅 init) / task.py(update+show, 无 usage flag)
- `cli/coding-supervisor/main.py` — typer 路由：archive / story init / task update / task show
- `PROJECT.md` 第 52 行 — 加入 coding-supervisor 入口，orchestrator 行标注"逐步废弃，由 coding-supervisor 替代"

## Remaining Tasks
1. **真实 story 试跑**：从现有 `docs/brainstorming/specs/` 挑一个小设计文档跑一遍 Phase 1→3 闭环，验收 inline 模式完整性
2. **2-3 个 story-summary.md 积累后** 回头调优：看负面机制是否需要砍、阈值（>10 files AND >1000 LOC）是否需要调
3. **待用户批准后** 可考虑删除 `cli/coding-orchestrator/` 和 `skills/coding-orchestrator/`（当前未删，reviewer agent 仍被 supervisor 相对引用）
4. **可选** git 提交：当前所有改动未提交，`git status` 可见

## Key Decisions
- **砍掉 orchestrator 6 条 references**：acceptance / constitution / dispatch-routes / storage-layout / story-intake / handoff-guideline。理由：supervisor 定位轻量，这些在 inline 模式下纯 ceremony
- **inline 作为默认模式**，阈值 `>10 files AND >1000 LOC` 才走 multi_wave。理由：实践中大部分任务都是小改，multi_wave 开销大于收益
- **inline 模式也强制派遣 reviewer sub-agent**。理由：review 不能自审自评，是质量底线
- **放弃 token 统计**（用户决定）。理由：inline 模式编排者自身 token 不可测
- **task.md 采用方案 A**（三段纯净，不含 Read First / File Scope / Worker Refs），multi_wave 额外上下文通过 dispatch prompt 拼接。理由：task.md 是合同，不是上下文载体
- **status 枚举从 6 个简化到 4 个**：删除 `reviewing` 和 `testing`。理由：review 是同步流程，executing→completed 直接翻转
- **Self-Evaluation 由 orchestrator 自己写**（用户决定，承认乐观偏差）。理由：派遣独立 sub-agent 审开销大
- **CLI 迁移而非复用**：`omp coding-supervisor` 完全自包含，不调 orchestrator。理由：用户要逐步废弃 orchestrator
- **reviewer agent 不复制**：supervisor SKILL.md 直接引用 `../coding-orchestrator/agents/code-reviewer.md`。理由：避免 agent 定义漂移
- **Phase 3 step 1 删除 "Rule 3 (Surgical Fix)" 引用**：该规则在 supervisor 精简版中不存在，改为"新建 fix task"
- **markdown skill 被遗漏**：在 SKILL.md 初稿写完后用户提醒才加载；之后做了诊断（Rule 3 bug + 结构评估），只修了 Rule 3 bug，未做结构重构

## Active Files
- `cli/coding-supervisor/main.py` — done
- `skills/coding-supervisor/SKILL.md` — done
- `skills/coding-supervisor/templates/*.{md,yaml}` — done（task.md / story.md / tasks.yaml）
- `skills/coding-supervisor/references/*.md` — done（4 个：task-decomposition / self-evaluation / story-memory-guideline / commands）
- `skills/coding-supervisor/scripts/*.py` — done（4 个：common / archive / story / task）
- `PROJECT.md` — done（仅第 52 行附近）
- 已删除：`skills/coding-orchestrator/templates/{handoff-context.yaml,story.md,task.md,tasks.yaml}`（git status 显示 D；事实上 supervisor 接收了这些 template 的对应文件，orchestrator 的 story.py 仍引用已不存在的 templates，若要继续使用 orchestrator 需恢复或切 path）

## Resume
Read .handover.md and continue 从 `docs/brainstorming/specs/` 挑一个小设计文档跑一遍 coding-supervisor 的 Phase 1→3 闭环验收

## Compaction Rules
- Turn -1 (last): remove tool results; keep user + assistant in full
- Turn -2: remove tool results; keep user in full; summarize assistant ≤100 chars
- Turn -3: remove tool results; keep user in full; summarize assistant ≤50 chars
