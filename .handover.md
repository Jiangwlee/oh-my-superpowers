---
context_pct: 70
timestamp: 2026-04-21T12:30
task_id: coding-orchestrator-optimization
pending: false
---

## Current Status

brainstorming 流程经历了两轮 reset:
1. **第一轮**:从 "coding-orchestrator 优化" 转向 "新建 `coding-supervisor` skill"
2. **第二轮(本次)**:用户推翻新建方向——"我们把 skill 做成了 system"、"dod 也不需要"——回归 **对 coding-orchestrator 做减法 + 微增量**

目前:方向锁定。下一步读 `skills/coding-orchestrator/SKILL.md`,定位三条增量的插入点,给具体 diff 方案。

## Final Decisions

### 方向

- **不新建 skill**;直接优化 `skills/coding-orchestrator/`
- 原则:**减法 + 微增量**,不造新 schema,不加基础设施

### 保留不动(orchestrator 现有结构)

- `handoff.md`(任务状态机表 + Active Task + Next Steps)
- `tasks.yaml`(所有现有字段:id/title/status/wave/depends_on/spec/files_modified/test_layer/worker/reviewer/started/completed/commits/notes)
- reviewer subagent(永远委派)
- story 目录结构(`stories/<story-id>/` 下 spec.md / tasks.yaml / handoff.md / tasks/ / reviews/)
- story-memory.md(现有跨 story 经验沉淀)

### 新增(三条工作流修改)

**1. cheap 探索步骤**(在 task 分解前强制):
- 受影响文件范围(输出文件列表)
- 关键函数调用链(防止只写代码忘记 wire 集成)
- 数据结构 + 数据流向(识别 schema 变动)

**2. mode 判断步骤**(探索后):
- **默认 inline**:orchestrator 自己 1 wave 完成
- **触发 multi_wave**:改动 > 10 文件 **且** 代码量 > 1000 行 → 委派 sub agent
- 阈值先拍,实际用后再调

**3. TDD 调整**:
- 砍默认 UT 流程
- E2E(chrome-devtools 实测)是真 DoD
- UT 下放 worker 判断("纯函数 + E2E 盖不到"时才写)

### 指导理论(不写入 skill,保留为 rationale)

orchestrator 真正在对抗三类问题:
1. 上下文污染 → 目标漂移
2. 外化知识(story-memory)
3. 渐进消解不确定性(JIT Spec)

这也是 cheap 探索 + mode 判断的价值基础——不是凭空规范,是让这三类对抗更显式。

## Ruled Out(防复燃,按 reset 深度排)

**第一轮 reset 砍掉**(system 层复杂度):
- ❌ `config.yaml` 自进化
- ❌ `triage.jsonl` / `evaluation.jsonl` 全局累积
- ❌ `memory.json` count 合并机制
- ❌ `omp skill coding-supervisor` CLI
- ❌ evaluator subagent
- ❌ 偏差分析 / stats
- ❌ handoff 的 frontmatter / evidence / checkpoint / worker notes 字段
- ❌ 任务后自评 + 独立 evaluator

**第二轮 reset 砍掉**(skill 层重做):
- ❌ 新建 `coding-supervisor` skill(改为优化 orchestrator)
- ❌ `dod.yaml` 三层 + 批准门(验收靠 reviewer + E2E,不单独形式化 DoD 文件)
- ❌ `.stories/` 全局目录(复用 orchestrator 现有 `stories/`)
- ❌ `memory.md` 新增(复用 orchestrator 现有 story-memory.md)
- ❌ checkpoint 机制(worker 遇决策走对话)
- ❌ 选项 A / B / C

**病根诊断**:
- 第一轮病根:skill 和 system 的边界没守住,每步 "+1" 累积成 10x
- 第二轮病根:新建 skill 本质是用新组织形式装 "减法 + 微增量",还是过度;直接改现有比建新更诚实

## Remaining Tasks

1. 读 `skills/coding-orchestrator/SKILL.md` 和 references,定位三条增量的插入点
2. 给出精确 diff 方案(哪几段改、怎么改)
3. 用户 review 后执行修改
4. 在真实 story 上试跑一次,观察 cheap 探索 + mode 判断是否帮助

## Active Files

- `skills/coding-orchestrator/` — 修改对象
- `stories/2026-04-19-gateway-lifecycle-unification/` — 参考实例,看现有工作流如何运转
- `~/.local/share/oh-my-superpowers/deep-research/2026-04-21T10-01-agent-orchestration-context/reports/` — 调研产出(本轮 reset 后大部分价值被砍;可忽略)

## Resume

Read `.handover.md`。方向:**对 `skills/coding-orchestrator/` 做减法 + 微增量**,
保留所有现有结构,只加三个工作流步骤(cheap 探索 / mode 判断 / TDD 调整)。

**下一步**:读 `skills/coding-orchestrator/SKILL.md` → 给具体 diff 方案。

## Compaction Rules
- Turn -1 (last): remove tool results; keep user + assistant in full
- Turn -2: remove tool results; keep user in full; summarize assistant ≤100 chars
- Turn -3: remove tool results; keep user in full; summarize assistant ≤50 chars
