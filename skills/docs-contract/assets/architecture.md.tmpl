---
doc-type: architecture
purpose: 系统整体架构 — 分层、模块边界、依赖方向
must-not-contain: [function-name, file-path, code-block, step-verb]
update-when: [新增系统分层, 模块边界变更, 跨模块依赖变更]
source-of-truth-for: [系统分层, 模块边界, 依赖方向]
defer-to: [decisions/]
---

# Architecture

## 分层

<!-- 用 ASCII / mermaid 表达，描述每层职责。不写具体实现。 -->

## 模块

<!-- 每个模块的职责一句话。细节进 modules/<name>.md。 -->

## 依赖方向

<!-- 哪些层依赖哪些层；明确禁止的反向依赖。 -->

## 跨模块协议

<!-- 协议契约入口；细节进 contracts/。 -->
