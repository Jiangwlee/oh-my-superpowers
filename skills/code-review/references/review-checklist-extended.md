# Extended Review Checklist
#
# 中大 diff（50+ 行）额外加载。与 core checklist 配合使用。
# 加载时机：diff >= 50 行时加载

## 3. Performance

- [ ] N+1 查询：循环内是否有数据库查询或网络请求？能否批量化？
- [ ] 无界循环/递归：是否有缺少终止条件或上限的循环？
- [ ] 缺失索引：新增的数据库查询是否利用了索引？WHERE 条件是否可能全表扫描？
- [ ] 不必要的内存分配：是否在循环中重复创建大对象？是否可以复用？
- [ ] 阻塞操作：I/O 密集操作是否在主线程/事件循环中同步执行？
- [ ] 缓存缺失：重复计算的结果是否可以缓存？

### Performance 启发式问题

- 如果数据量增长 100 倍，这段代码还能正常工作吗？
- 这个操作的时间复杂度是什么？有没有更优的算法？

## 4. Maintainability

- [ ] 命名清晰度：变量名/函数名是否准确表达其含义？是否有单字母变量（循环计数器除外）？
- [ ] 函数长度：单个函数是否超过 50 行？是否可以拆分？
- [ ] 嵌套深度：是否有超过 3 层的嵌套？能否用 early return 简化？
- [ ] 重复代码：是否有 3 处以上的相似代码片段？（注意：2 处相似不需要抽象）
- [ ] 错误处理：异常是否被合理处理？错误信息是否对调试有帮助？
- [ ] 代码异味：God class、feature envy、primitive obsession、long parameter list？

### Maintainability 启发式问题

- 一个新人读这段代码，能否在 5 分钟内理解它的作用？
- 如果需要修改这段代码的行为，改动范围有多大？

## 5. SOLID Violations

> 仅在 diff 涉及类/模块设计时检查。对小改动（单函数修改、bug fix）跳过本节。

- [ ] **S — 单一职责：** 这个类/模块是否只有一个变更理由？
- [ ] **O — 开闭原则：** 新增功能是否通过扩展实现，而非修改已有代码？
- [ ] **L — 里氏替换：** 子类是否可以安全替换父类？
- [ ] **I — 接口隔离：** 调用方是否被迫依赖它不需要的方法？
- [ ] **D — 依赖反转：** 高层模块是否直接依赖了具体实现而非抽象？

> 注意：不要为了满足 SOLID 而过度设计。只在违反导致实际问题时标记。
